# Recommendation engine Udacity project

The resulting notebook is in `notebooks/Recommendations_with_IBM.ipynb` and the transformed version is in the same folder and under the same name in `.html` format.